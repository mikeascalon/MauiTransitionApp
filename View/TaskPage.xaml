<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:vm="clr-namespace:TransitionApp.ViewModel"
             x:Class="TransitionApp.View.TaskPage"
             Title="Task Page">
   

    <StackLayout Padding="10">
        <Label Text="Tasks" FontSize="20" FontAttributes="Bold" />
        <CollectionView ItemsSource="{Binding Tasks}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Horizontal" Padding="5">
                        <Label Text="{Binding Name}" FontSize="16" />
                        <Button Text="Edit" Command="{Binding Source={RelativeSource AncestorType={x:Type vm:TaskViewModel}}, Path=UpdateCommand}" CommandParameter="{Binding .}" />
                        <Button Text="Delete" Command="{Binding Source={RelativeSource AncestorType={x:Type vm:TaskViewModel}}, Path=DeleteCommand}" CommandParameter="{Binding TaskId}" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Text="Add Task" Command="{Binding AddCommand}" />
    </StackLayout>
</ContentPage>